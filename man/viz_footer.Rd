% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layers.R
\name{viz_footer}
\alias{viz_footer}
\title{Source of the map}
\usage{
viz_footer(
  map,
  id = NULL,
  text = "Author, source...",
  fill = "#9e9696",
  background_fill = "white",
  background_stroke = "white",
  background_strokeWidth = 1,
  dominantBaseline = "central",
  textAnchor = "middle",
  lineSpacing = 0,
  margin = 1,
  fontSize = 10,
  fontFamily = NULL,
  dx = 0,
  dy = 0,
  ...
)
}
\arguments{
\item{map}{A \code{geoviz} map created with \code{viz_create}.}

\item{id}{character. Optional. Unique layer id.}

\item{text}{character. Optional. Footer text to display (default "Author, source...").}

\item{fill}{character. Optional. Text color (default "#9e9696").}

\item{background_fill}{character. Optional. Background fill color (default "white").}

\item{background_stroke}{character. Optional. Background stroke color (default "white").}

\item{background_strokeWidth}{numeric. Optional. Background stroke width (default 1).}

\item{dominantBaseline}{character. Optional. Vertical text alignment (default "central", values "hanging","middle","central","bottom").}

\item{textAnchor}{character. Optional. Horizontal text alignment (default "middle", values "start","middle","end").}

\item{lineSpacing}{numeric. Optional. Line spacing (default 0).}

\item{margin}{numeric. Optional. Margin (default 1).}

\item{fontSize}{numeric. Optional. Font size (default 10).}

\item{fontFamily}{character. Optional. Font family (default container font).}

\item{dx}{numeric. Optional. X shift (default 0).}

\item{dy}{numeric. Optional. Y shift (default 0).}

\item{...}{Additional SVG attributes applied to text and background elements.}
}
\description{
The \code{viz_footer} function adds a text below a geoviz map.
}
\examples{
library(sf)
world <- st_read(system.file("gpkg/world.gpkg", package = "geoviz"),
  quiet = TRUE)
viz_create(projection = "EqualEarth", background = "white") |>
  viz_path(datum = world, fill = "#f1f3f5") |>
  viz_footer(text = "Source, author, note, etc.", fill = "#38896F" ) |>
  viz_render()
}
