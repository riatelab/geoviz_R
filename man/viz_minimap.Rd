% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layers.R
\name{viz_minimap}
\alias{viz_minimap}
\title{Minimap inset layer}
\usage{
viz_minimap(
  map,
  id = NULL,
  basemap_data = NULL,
  basemap_fill = "white",
  basemap_fillOpacity = 0.5,
  basemap_stroke = "none",
  width = 200,
  projection = "EqualEarth",
  precision = 10,
  pos = c(10, 10),
  location_type = "polygon",
  location_r = 5,
  location_fill = NULL,
  location_stroke = NULL,
  location_strokeWidth = 1.2,
  domain = NULL,
  margin = NULL,
  ...
)
}
\arguments{
\item{map}{A \code{geoviz} map created with \code{viz_create}.}

\item{id}{character. Optional. Unique layer id. If NULL, a random id is generated.}

\item{basemap_data}{object. Optional. GeoJSON basemap. Default is \code{land}.}

\item{basemap_fill}{character. Optional. Fill color of the basemap (default "white").}

\item{basemap_fillOpacity}{numeric. Optional. Fill opacity of the basemap (default 0.5).}

\item{basemap_stroke}{character. Optional. Stroke color of the basemap (default "none").}

\item{width}{numeric. Optional. Width of the minimap (default 200).}

\item{projection}{character. Optional. Projection used for the minimap (default "EqualEarth").}

\item{precision}{numeric. Optional. Geometry simplification precision (default 10).}

\item{pos}{numeric vector. Optional. Position of the minimap (default c(10, 10)).}

\item{location_type}{character. Optional. Type of location geometry: "polygon" or "point" (default "polygon").}

\item{location_r}{numeric. Optional. Radius when location_type = "point" (default 5).}

\item{location_fill}{character. Optional. Fill color of the location geometry.}

\item{location_stroke}{character. Optional. Stroke color of the location geometry.}

\item{location_strokeWidth}{numeric. Optional. Stroke width of the location geometry (default 1.2).}

\item{domain}{list. Optional. Projection domain.}

\item{margin}{list. Optional. Margin configuration.}

\item{...}{Additional styling options passed as prefixed arguments.
Supported prefixes include:
\code{outline_*} for outline styling properties,
\code{basemap_*} for basemap styling properties,
\code{location_*} for location geometry styling properties.}
}
\description{
The \code{viz_minimap} function adds a minimap inset to a geoviz map.
It displays a basemap and optionally highlights a location geometry (polygon or point).
This is useful for showing spatial context in map layouts.
NB: The map’s projection must provide an invert() function.
}
\examples{
library(sf)
world <- st_read(
  system.file("gpkg/world.gpkg", package = "geoviz"),
  quiet = TRUE
)
afr <- world[world$region == "Africa", ]
viz_create(projection = "Mercator", background = "white", domain = afr) |>
  viz_path(data = world, fill = "#9e9696") |>
  viz_minimap(
    width = 200,
    projection = "EqualEarth",
    pos = c(20, 20),
    location_stroke = "red"
  ) |>
  viz_render()
}
