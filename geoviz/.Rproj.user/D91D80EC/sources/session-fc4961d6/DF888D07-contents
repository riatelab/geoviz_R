create <- function(x, width = NULL, height = NULL, elementId = NULL) {
  htmlwidgets::createWidget(
    x = x,
    name = 'create',
    width = width,
    height = height,
    package = 'rgeoviz',
    elementId = elementId
  )
}
